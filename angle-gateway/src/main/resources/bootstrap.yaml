spring:
  cloud:
    config:
      profile: dev
      name: gateway
      discovery:
        enabled: true
        service-id: angle-config-server
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10010/eureka/