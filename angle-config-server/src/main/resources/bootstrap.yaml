spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ywendy/angle.git
          search-paths: config/sys,config/user
          username:
          password: